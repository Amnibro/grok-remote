# Guardian council: composer restructure + watch

| Guardian | Approach | Vote |
|----------|----------|------|
| Architect | + = attach; mic next to box; specials + hide toggles in ☰; XR caps autodetect; `/watch` companion | yes |
| Sentinel | Don’t auto-enter XR on desktop; only wearable/prior mode; watch page read-only-safe reconnect | yes |
| Scholar | data-kind on tools for hide-edit/hide-read; body classes for CSS hide | yes |
| Engineer | Move injectChrome tools into moreMenu; slim git strip stays in footer | yes |
| Pathfinder | Galaxy Watch = Wear OS browser PWA at /watch (no native store pipeline required) | yes |

## Decision

Majority: full restructure as Architect+Engineer; XR prefer AR when available; watch as lightweight web companion.
