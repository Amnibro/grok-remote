# Guardian Council · Spoiler-safe public screenshots

## Architect
Ship a `?demo=1` capture path with scrubbed sessions + forced Grok theme so README never depends on live disk state.

## Sentinel
Blur must cover titles not just paths; mobile must never advertise Alt; no username/cwd/chat titles from real sessions in git.

## Scholar
Prior shots failed because title blur landed after capture and localStorage kept Scient; document demo URL in README.

## Engineer
Reuse existing privacy CSS; add spoiler classes on titles; `isLinked()` for demo rail; Playwright + Chrome for PNGs.

## Pathfinder
Majority: demo mode + retake all PNGs + README. Alternate (edit PNGs by hand) rejected as fragile.

**Ruling:** Demo capture pipeline + stronger spoiler + Grok default shots.
